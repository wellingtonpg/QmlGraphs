#ifndef GRAPHBASE_H
#define GRAPHBASE_H

#include <QObject>

class GraphBase : public QObject
{
    Q_OBJECT
    
    Q_PROPERTY(QString name READ name WRITE setname NOTIFY nameChanged)
    
public:
    explicit GraphBase(QObject *parent = 0);

signals:

public slots:
};

#endif // GRAPHBASE_H
